overwrite: true
schema:
  - "https://tuggecli.api.sanity.io/v1/graphql/production/default"
generates:
  ./src/generated/graphql.ts:
    plugins:
      - "typescript"
    config:
      skipTypename: true