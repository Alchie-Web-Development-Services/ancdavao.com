overwrite: true
schema:
  - "https://tuggecli.api.sanity.io/v1/graphql/production/default"
documents: "./src/**/*.graphql"
generates:
  ./src/generated/graphql.ts:
    plugins:
      - add: { content: "/* eslint-disable */\n// @ts-nocheck" }
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
    config:
      rawRequest: true
      skipTypename: true