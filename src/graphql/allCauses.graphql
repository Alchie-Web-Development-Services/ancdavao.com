query AllCauses {
  allCause {
    _id
    _type
    title
    slug {
      current
    }
    startDate
    endDate
    goalAmount
    descriptionRaw
    mainImage {
      asset {
        _id
        url
      }
    }
  }
}
