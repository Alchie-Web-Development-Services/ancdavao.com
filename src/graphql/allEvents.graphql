query AllEvents {
  allEvent {
    _id
    _type
    title
    slug {
      current
    }
    startDate
    endDate
    location
    descriptionRaw
    mainImage {
      asset {
        _id
        url
      }
    }
  }
}