query CauseById($id: ID!) {
  Cause(id: $id) {
    _id
    _type
    title
    slug {
      current
    }
    startDate
    endDate
    goalAmount
    raised
    descriptionRaw
    mainImage {
      asset {
        _id
        url
      }
    }
  }
}