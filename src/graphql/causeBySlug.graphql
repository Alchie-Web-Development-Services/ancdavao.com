query CauseBySlug($slug: String!) {
  allCause(where: {slug: {current: {eq: $slug}}}) {
    _id
    _type
    title
    slug {
      current
    }
    startDate
    endDate
    goalAmount
    raised
    descriptionRaw
    mainImage {
      asset {
        _id
        url
      }
    }
  }
}